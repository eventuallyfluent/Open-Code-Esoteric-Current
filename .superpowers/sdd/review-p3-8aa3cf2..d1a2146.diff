# Review package: 8aa3cf2..d1a2146

## Commits
d1a2146 feat(worker): scaffold ΓÇö package.json, env config, logger, entry point

## Files changed
 worker/.env.example        | 10 ++++++++++
 worker/package.json        | 17 +++++++++++++++++
 worker/src/config.js       | 20 ++++++++++++++++++++
 worker/src/index.js        | 21 +++++++++++++++++++++
 worker/src/utils/logger.js | 11 +++++++++++
 5 files changed, 79 insertions(+)

## Diff
diff --git a/worker/.env.example b/worker/.env.example
new file mode 100644
index 0000000..6802d69
--- /dev/null
+++ b/worker/.env.example
@@ -0,0 +1,10 @@
+# Required
+DEEPSEEK_API_KEY=sk-your-key-here
+WORDPRESS_URL=https://theesotericcurrent.com
+WORDPRESS_API_KEY=your-ec-api-key
+WORDPRESS_API_SECRET=your-ec-api-secret
+
+# Optional
+DEEPSEEK_MODEL=deepseek-chat
+LOG_LEVEL=info
+DRY_RUN=false
diff --git a/worker/package.json b/worker/package.json
new file mode 100644
index 0000000..d41cbc7
--- /dev/null
+++ b/worker/package.json
@@ -0,0 +1,17 @@
+{
+  "name": "esoteric-current-worker",
+  "version": "1.0.0",
+  "private": true,
+  "type": "module",
+  "description": "One-shot automation worker for The Esoteric Current ΓÇö runs in GitHub Actions",
+  "main": "src/index.js",
+  "scripts": {
+    "start": "node src/index.js",
+    "test": "node --test tests/**/*.test.js",
+    "dry-run": "node src/index.js --dry-run"
+  },
+  "engines": {
+    "node": ">=20.0.0"
+  },
+  "devDependencies": {}
+}
diff --git a/worker/src/config.js b/worker/src/config.js
new file mode 100644
index 0000000..0277943
--- /dev/null
+++ b/worker/src/config.js
@@ -0,0 +1,20 @@
+import { env } from 'node:process';
+
+const required = ['DEEPSEEK_API_KEY', 'WORDPRESS_URL', 'WORDPRESS_API_KEY', 'WORDPRESS_API_SECRET'];
+
+export function loadConfig() {
+  const missing = required.filter(k => !env[k]);
+  if (missing.length > 0) {
+    throw new Error(`Missing required env vars: ${missing.join(', ')}`);
+  }
+  return Object.freeze({
+    deepseekApiKey: env.DEEPSEEK_API_KEY,
+    deepseekModel: env.DEEPSEEK_MODEL || 'deepseek-chat',
+    wordpressUrl: env.WORDPRESS_URL.replace(/\/+$/, ''),
+    wordpressApiKey: env.WORDPRESS_API_KEY,
+    wordpressApiSecret: env.WORDPRESS_API_SECRET,
+    logLevel: env.LOG_LEVEL || 'info',
+    dryRun: env.DRY_RUN === 'true' || process.argv.includes('--dry-run'),
+    maxRetries: Number(env.MAX_RETRIES || '3'),
+  });
+}
diff --git a/worker/src/index.js b/worker/src/index.js
new file mode 100644
index 0000000..c00cbd4
--- /dev/null
+++ b/worker/src/index.js
@@ -0,0 +1,21 @@
+import { loadConfig } from './config.js';
+import { createLogger } from './utils/logger.js';
+
+async function main() {
+  const config = loadConfig();
+  const log = createLogger(config.logLevel);
+
+  log.info('worker-start', { dryRun: config.dryRun });
+
+  // Phase 1: Research ΓÇö discover topics via DeepSeek
+  // Phase 2: Collect ΓÇö deep-dive on each topic
+  // Phase 3: Synthesize ΓÇö format into articles
+  // Phase 4: Submit ΓÇö POST to WordPress REST API
+
+  log.info('worker-complete');
+}
+
+main().catch(err => {
+  console.error(JSON.stringify({ l: 'error', t: new Date().toISOString(), m: err.message, stack: err.stack }));
+  process.exit(1);
+});
diff --git a/worker/src/utils/logger.js b/worker/src/utils/logger.js
new file mode 100644
index 0000000..7a432c2
--- /dev/null
+++ b/worker/src/utils/logger.js
@@ -0,0 +1,11 @@
+const levels = { error: 0, warn: 1, info: 2, debug: 3 };
+
+export function createLogger(level = 'info') {
+  const threshold = levels[level] ?? levels.info;
+  return {
+    error: (...args) => threshold >= 0 && console.error(JSON.stringify({ l: 'error', t: new Date().toISOString(), m: args.join(' ') })),
+    warn:  (...args) => threshold >= 1 && console.warn( JSON.stringify({ l: 'warn',  t: new Date().toISOString(), m: args.join(' ') })),
+    info:  (...args) => threshold >= 2 && console.log(  JSON.stringify({ l: 'info',  t: new Date().toISOString(), m: args.join(' ') })),
+    debug: (...args) => threshold >= 3 && console.log(  JSON.stringify({ l: 'debug', t: new Date().toISOString(), m: args.join(' ') })),
+  };
+}
